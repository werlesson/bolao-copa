server {
    listen 80;
    server_name localhost;
    root /var/www/html/public;
    index index.php index.html;

    charset utf-8;

    # Logs
    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log;

    # Redireciona tudo para o Laravel
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP-FPM
    location ~ \.php$ {
        fastcgi_pass   app:9000;
        fastcgi_index  index.php;
        # Caminho absoluto do PHP-FPM (container app) — não depende dos arquivos estarem no Nginx
        fastcgi_param  SCRIPT_FILENAME /var/www/html/public$fastcgi_script_name;
        # Repassa o IP real do cliente (definido pelo Traefik/load balancer upstream)
        fastcgi_param  HTTP_X_FORWARDED_FOR $http_x_forwarded_for;
        fastcgi_param  HTTP_X_REAL_IP       $http_x_real_ip;
        include        fastcgi_params;
        fastcgi_read_timeout 300;
    }

    # Bloquear acesso a arquivos ocultos
    location ~ /\.(?!well-known).* {
        deny all;
    }

    # Upload máximo
    client_max_body_size 10M;

    # Compressão gzip para respostas JSON da API
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types application/json application/javascript text/css text/plain;
}
